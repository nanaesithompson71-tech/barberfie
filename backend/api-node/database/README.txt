These are copies of ../../database/schema.sql and seed.sql, bundled so the API can
create its own tables on hosts (like Railway) that only deploy the api-node folder.
If you change the originals, copy them here again.
